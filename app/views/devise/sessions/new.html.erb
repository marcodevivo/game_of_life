<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Game of Life</title>
  <style>
      body {
          font-family: Arial, sans-serif;
          background-color: #121212;
          color: white;
          text-align: center;
          margin: 0;
          padding: 0;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          height: 100vh;
      }
      .container {
          background: rgba(255, 255, 255, 0.1);
          padding: 20px;
          border-radius: 10px;
          box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
          width: 100%;
          max-width: 400px;
      }
      h1 {
          font-size: 3rem;
          margin-bottom: 20px;
      }
      p {
          font-size: 1.2rem;
          margin-bottom: 20px;
      }
      .btn {
          background: #ff6600;
          color: white;
          padding: 10px 20px;
          font-size: 1.2rem;
          text-decoration: none;
          border-radius: 5px;
          transition: 0.3s;
          display: inline-block;
          margin-top: 20px;
      }
      .btn:hover {
          background: #cc5200;
      }
      .input-field {
          background-color: #333;
          color: white;
          border: 1px solid #555;
          padding: 10px;
          border-radius: 5px;
          width: 80%; /* Ridotto a 80% per i campi */
          margin-bottom: 15px; /* Spazio tra i campi */
      }
      .input-field:focus {
          outline: none;
          border-color: #ff6600;
      }
      .form-group {
          display: flex;
          flex-direction: column;
          align-items: center;  /* Centrato sia la label che il campo */
          margin-bottom: 15px;
      }
      .form-group label {
          font-size: 1.2rem;
          margin-bottom: 5px;
      }
      .form-group label:first-child {
          margin-top: 10px;  /* Aggiunge spazio sopra la label email */
      }
      .error-message {
          color: red;
          font-size: 0.9rem;
          margin-top: 10px;
      }
  </style>
</head>
<body>
<div class="container">
  <h1>Login</h1>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

    <div class="form-group">
      <%= f.label :email, "Email" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Email", class: "input-field" %>
    </div>

    <div class="form-group">
      <%= f.label :password, "Password" %>
      <%= f.password_field :password, autocomplete: "current-password", placeholder: "Password", class: "input-field" %>
    </div>

    <% if resource.errors.any? %>
      <div class="error-message">
        <ul>
          <% resource.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= f.submit "Accedi", class: "btn" %>
  <% end %>

  <!--<p><a href="<%#= new_user_registration_path %>" class="btn">Registrati</a></p>-->

  <p><a href="<%= root_path %>" class="btn" style="background-color: #444;">Indietro</a></p>
</div>
</body>
</html>
